# Workflow

## После изменения
1. `npx tsc --noEmit`
2. Логика верна?
3. Edge cases?

**Fail → Fix → Verify**

## UI изменения
1. `Grep("ComponentName")` — проверь существующее
2. shadcn/ui, не native HTML
3. CSS переменные, не hex
